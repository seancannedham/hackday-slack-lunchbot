/**
 * Created by Alexander Jakel on 2/2/2018.
 */

//takes in the input and determines which command needs to be run.
function interpreter (user, command, data)
{
    switch (command)
    {
        case "/vote":
            //call vote function
            break;
        case "/results":
            //show all results
            break;
    }
}

function addVote(user, vote)
{
    //check user has not voted twice
    //check location is valid


}